name: meow-ios

options:
  bundleIdPrefix: com.tangzixiang.meow
  deploymentTarget:
    iOS: "18.0"
    # NEPacketTunnelProvider landed on tvOS in 17.0 — there is no earlier
    # floor worth supporting because the whole app is the tunnel.
    tvOS: "17.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  groupSortPosition: top
  xcodeVersion: "26.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    SUPPORTS_MACCATALYST: NO
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: NO
    SWIFT_STRICT_CONCURRENCY: complete
    CLANG_CXX_LANGUAGE_STANDARD: "c++17"
    GCC_C_LANGUAGE_STANDARD: c11
    ENABLE_MODULE_VERIFIER: NO

packages:
  MeowShared:
    path: MeowShared
  Yams:
    url: https://github.com/jpsim/Yams.git
    from: "5.1.3"
targets:
  meow-ios:
    type: application
    platform: iOS
    sources:
      - path: App/Sources
      - path: App/Resources
        optional: true
        excludes:
          - "**/*.md"
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: meow
        CFBundleShortVersionString: "1.7.0"
        CFBundleVersion: "2026091701"
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UILaunchScreen:
          UIColorName: LaunchBackground
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
          NSAllowsLocalNetworking: true
        NSCameraUsageDescription: "meow uses the camera to scan Shadowsocks server QR codes."
        ITSAppUsesNonExemptEncryption: false
        CFBundleURLTypes:
          - CFBundleURLName: com.tangzixiang.meow.deeplink
            CFBundleURLSchemes:
              - meow
    entitlements:
      path: App/App.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tangzixiang.meow
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.tangzixiang.meow
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tangzixiang.meow
        # Alternate Home Screen icons (Settings → App Icon). Each name is an
        # .appiconset in Assets.xcassets; actool injects the matching
        # CFBundleIcons entries into the processed Info.plist.
        ASSETCATALOG_COMPILER_ALTERNATE_APPICON_NAMES: AppIconLeap AppIconSunset AppIconMidnight
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: YES
        SUPPORTED_PLATFORMS: "iphoneos iphonesimulator macosx"
        CODE_SIGN_ENTITLEMENTS: App/App.entitlements
        SWIFT_OBJC_BRIDGING_HEADER: App/Sources/BridgingHeader.h
        GENERATE_INFOPLIST_FILE: NO
        CLANG_ENABLE_MODULES: YES
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/Frameworks
        OTHER_LDFLAGS:
          - -ObjC
          # boring-sys (via meow-transport boring-tls feature) vendors
          # BoringSSL C++ — link libc++ so std::__1 symbols resolve.
          - -lc++
    dependencies:
      - target: PacketTunnel
        embed: true
        codeSign: true
      - package: MeowShared
        product: MeowModels
      - package: MeowShared
        product: MeowIPC
      - package: Yams
      - framework: MeowCore/Frameworks/MeowCore.xcframework
        embed: false
        optional: true
      - sdk: SystemConfiguration.framework
    postBuildScripts:
      - name: Strip Release binary (strip -Sx)
        runOnlyWhenInstalling: false
        basedOnDependencyAnalysis: false
        script: |
          if [ "$CONFIGURATION" = "Release" ]; then
            /usr/bin/xcrun strip -Sx "$TARGET_BUILD_DIR/$EXECUTABLE_PATH"
          fi

  MeowTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: MeowTests
        excludes:
          - "README.md"
          - "Fixtures/README.md"
    dependencies:
      - target: meow-ios
      - package: MeowShared
        product: MeowModels
      - package: MeowShared
        product: MeowIPC
      - package: Yams
    settings:
      base:
        BUNDLE_LOADER: $(TEST_HOST)
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/meow-ios.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/meow-ios
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_OBJC_BRIDGING_HEADER: App/Sources/BridgingHeader.h
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include

  MeowUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: MeowUITests
        excludes:
          - "README.md"
    dependencies:
      - target: meow-ios
      - package: MeowShared
        product: MeowModels
      - package: MeowShared
        product: MeowIPC
    settings:
      base:
        TEST_TARGET_NAME: meow-ios
        GENERATE_INFOPLIST_FILE: YES

  MeowIntegrationTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: MeowIntegrationTests
        excludes:
          - "README.md"
    dependencies:
      - target: meow-ios
      - package: MeowShared
        product: MeowModels
      - package: MeowShared
        product: MeowIPC
    settings:
      base:
        BUNDLE_LOADER: $(TEST_HOST)
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/meow-ios.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/meow-ios
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_OBJC_BRIDGING_HEADER: App/Sources/BridgingHeader.h
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include

  PacketTunnel:
    type: app-extension
    platform: iOS
    sources:
      - path: PacketTunnel/Sources
    info:
      path: PacketTunnel/Info.plist
      properties:
        CFBundleDisplayName: meow Tunnel
        CFBundleShortVersionString: "1.7.0"
        CFBundleVersion: "2026091701"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.networkextension.packet-tunnel
          NSExtensionPrincipalClass: PacketTunnelProvider
    entitlements:
      path: PacketTunnel/PacketTunnel.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tangzixiang.meow
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.tangzixiang.meow
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tangzixiang.meow.PacketTunnel
        CODE_SIGN_ENTITLEMENTS: PacketTunnel/PacketTunnel.entitlements
        GENERATE_INFOPLIST_FILE: NO
        CLANG_ENABLE_MODULES: YES
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/Frameworks
        OTHER_LDFLAGS:
          - -ObjC
          # boring-sys (via meow-transport boring-tls feature) vendors
          # BoringSSL C++ — link libc++ so std::__1 symbols resolve.
          - -lc++
    dependencies:
      - framework: MeowCore/Frameworks/MeowCore.xcframework
        embed: false
        optional: true
      - sdk: SystemConfiguration.framework
    postBuildScripts:
      - name: Strip Release binary (strip -Sx)
        runOnlyWhenInstalling: false
        basedOnDependencyAnalysis: false
        script: |
          if [ "$CONFIGURATION" = "Release" ]; then
            /usr/bin/xcrun strip -Sx "$TARGET_BUILD_DIR/$EXECUTABLE_PATH"
          fi

  meow-tvos:
    type: application
    platform: tvOS
    sources:
      - path: AppTV/Sources
      # The tvOS app reuses the model + service layer verbatim and replaces
      # only the view layer. Nothing under Models/ or Services/ imports UIKit
      # or SwiftUI, so it compiles for tvOS unchanged — see AppTV/README.md.
      - path: App/Sources/Models
      - path: App/Sources/Services
      - path: App/Sources/AppModel.swift
      - path: App/Sources/AppModelContainer.swift
      # tvOS's own catalog: the accent and launch colors (copies of the iOS
      # ones) and, once it ships, the Brand Assets stack.
      - path: AppTV/Assets.xcassets
      # Localizable.strings and the GeoIP/geosite databases are shared;
      # Assets.xcassets is not — it holds iPhone Home Screen icon sets, and
      # tvOS wants a Brand Assets stack instead (not shipped in this MVP).
      - path: App/Resources
        optional: true
        excludes:
          - "**/*.md"
          - "Assets.xcassets"
    info:
      path: AppTV/Info.plist
      properties:
        CFBundleDisplayName: meow
        UILaunchScreen:
          UIColorName: LaunchBackground
        CFBundleShortVersionString: "1.5.0"
        CFBundleVersion: "2026080601"
        UIRequiredDeviceCapabilities:
          - arm64
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
          NSAllowsLocalNetworking: true
        ITSAppUsesNonExemptEncryption: false
    entitlements:
      path: AppTV/AppTV.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tangzixiang.meow
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.tangzixiang.meow
    settings:
      base:
        # Same bundle ID as the iOS app on purpose: one App Store record and
        # universal purchase across iPhone/iPad and Apple TV. The two never
        # collide because they ship as separate platform binaries.
        PRODUCT_BUNDLE_IDENTIFIER: com.tangzixiang.meow
        TVOS_DEPLOYMENT_TARGET: "17.0"
        TARGETED_DEVICE_FAMILY: "3"
        SUPPORTED_PLATFORMS: "appletvos appletvsimulator"
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        CODE_SIGN_ENTITLEMENTS: AppTV/AppTV.entitlements
        SWIFT_OBJC_BRIDGING_HEADER: App/Sources/BridgingHeader.h
        GENERATE_INFOPLIST_FILE: NO
        CLANG_ENABLE_MODULES: YES
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/Frameworks
        OTHER_LDFLAGS:
          - -ObjC
          # boring-sys (via meow-transport boring-tls feature) vendors
          # BoringSSL C++ — link libc++ so std::__1 symbols resolve.
          - -lc++
    dependencies:
      - target: PacketTunnelTV
        embed: true
        codeSign: true
      - package: MeowShared
        product: MeowModels
      - package: MeowShared
        product: MeowIPC
      - package: Yams
      - framework: MeowCore/Frameworks/MeowCore.xcframework
        embed: false
        optional: true
      - sdk: SystemConfiguration.framework
    postBuildScripts:
      - name: Strip Release binary (strip -Sx)
        runOnlyWhenInstalling: false
        basedOnDependencyAnalysis: false
        script: |
          if [ "$CONFIGURATION" = "Release" ]; then
            /usr/bin/xcrun strip -Sx "$TARGET_BUILD_DIR/$EXECUTABLE_PATH"
          fi

  PacketTunnelTV:
    type: app-extension
    platform: tvOS
    # Byte-for-byte the same provider as iOS: PacketTunnel/Sources is pure
    # Objective-C over NetworkExtension and the Rust FFI, with no UIKit and no
    # iOS-only API in it, so tvOS needs a target but not a fork.
    sources:
      - path: PacketTunnel/Sources
    info:
      path: PacketTunnelTV/Info.plist
      properties:
        CFBundleDisplayName: meow Tunnel
        CFBundleShortVersionString: "1.5.0"
        CFBundleVersion: "2026080601"
        UIRequiredDeviceCapabilities:
          - arm64
        NSExtension:
          NSExtensionPointIdentifier: com.apple.networkextension.packet-tunnel
          NSExtensionPrincipalClass: PacketTunnelProvider
    entitlements:
      path: PacketTunnelTV/PacketTunnelTV.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.tangzixiang.meow
        com.apple.developer.networking.networkextension:
          - packet-tunnel-provider
        keychain-access-groups:
          - $(AppIdentifierPrefix)com.tangzixiang.meow
    settings:
      base:
        # Matches the iOS extension's identifier so the hardcoded
        # `providerBundleIdentifier` in VpnManager.configureIfNeeded stays
        # correct on both platforms.
        PRODUCT_BUNDLE_IDENTIFIER: com.tangzixiang.meow.PacketTunnel
        TVOS_DEPLOYMENT_TARGET: "17.0"
        TARGETED_DEVICE_FAMILY: "3"
        SUPPORTED_PLATFORMS: "appletvos appletvsimulator"
        CODE_SIGN_ENTITLEMENTS: PacketTunnelTV/PacketTunnelTV.entitlements
        GENERATE_INFOPLIST_FILE: NO
        CLANG_ENABLE_MODULES: YES
        HEADER_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/include
        LIBRARY_SEARCH_PATHS:
          - $(SRCROOT)/MeowCore/Frameworks
        OTHER_LDFLAGS:
          - -ObjC
          # boring-sys (via meow-transport boring-tls feature) vendors
          # BoringSSL C++ — link libc++ so std::__1 symbols resolve.
          - -lc++
    dependencies:
      - framework: MeowCore/Frameworks/MeowCore.xcframework
        embed: false
        optional: true
      - sdk: SystemConfiguration.framework
    postBuildScripts:
      - name: Strip Release binary (strip -Sx)
        runOnlyWhenInstalling: false
        basedOnDependencyAnalysis: false
        script: |
          if [ "$CONFIGURATION" = "Release" ]; then
            /usr/bin/xcrun strip -Sx "$TARGET_BUILD_DIR/$EXECUTABLE_PATH"
          fi

schemes:
  meow-ios:
    build:
      targets:
        meow-ios: all
        PacketTunnel: all
        MeowTests: [test]
        MeowUITests: [test]
        MeowIntegrationTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - MeowTests
        - MeowUITests
        - MeowIntegrationTests
      gatherCoverageData: true
      coverageTargets:
        - meow-ios
    archive:
      config: Release

  meow-tvos:
    build:
      targets:
        meow-tvos: all
        PacketTunnelTV: all
    run:
      config: Debug
    archive:
      config: Release
